	/**
	 * Constructor
	 *
	 * @param code
	 *              The error code
	 * @param reason
	 *              The reason to reject the connection
	 */
    public ConnectionRejected(int code, String reason)
    {
		this.code = code;
		this.reason = reason;
    }

