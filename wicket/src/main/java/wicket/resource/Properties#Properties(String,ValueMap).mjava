	/**
	 * Construct
	 * 
	 * @param key
	 *            The key
	 * @param strings
	 *            Properties values
	 */
	Properties(final String key, final ValueMap strings)
	{
		this.key = key;
		this.strings = strings;
	}

