	/**
	 * Construct.
	 * 
	 * @param id
	 *            throttle id
	 * @param delay
	 *            throttle delay
	 */
	public AjaxCallThrottlingDecorator(String id, Duration delay)
	{
		this(null, id, delay);
	}

