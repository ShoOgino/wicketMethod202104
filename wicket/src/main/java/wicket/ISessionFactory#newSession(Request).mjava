	/**
	 * Creates a new session
	 * 
	 * @param request
	 *            The request that will create this session.
	 * 
	 * @return The session
	 * 
	 * @since 1.3
	 */
	Session newSession(Request request);

