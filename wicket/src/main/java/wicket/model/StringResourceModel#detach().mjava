	/**
	 * Detaches from the given session
	 */
	@Override
	public final void detach()
	{
		// Detach any model
		if (model != null)
		{
			model.detach();
		}

		// Null out references
		this.localizer = null;
		this.locale = null;

		super.detach();
	}

