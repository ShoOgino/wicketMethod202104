		/**
		 * Construct.
		 * 
		 * @param fragment
		 * @param index
		 */
		public LookupResult(final MarkupFragment fragment, final int index)
		{
			this.fragment = fragment;
			this.index = index;
		}

