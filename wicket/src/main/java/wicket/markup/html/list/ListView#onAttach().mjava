	/**
	 * @see wicket.Component#onAttach()
	 */
	@Override
	protected void onAttach()
	{
		super.onAttach();

		if (isVisibleInHierarchy())
		{
			if (getViewSize() > 0)
			{
				onGenerateItems();
			}
			else
			{
				removeAll();
			}
		}
	}

