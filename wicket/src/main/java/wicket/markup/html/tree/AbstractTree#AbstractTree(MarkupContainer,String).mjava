	/**
	 * Tree constructor
	 * 
	 * @param parent
	 *            The parent component
	 * @param id
	 *            The component id
	 */
	public AbstractTree(MarkupContainer parent, String id)
	{
		super(parent, id);
		init();
	}

