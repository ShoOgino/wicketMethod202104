	/**
	 * Tree contructor.
	 * 
	 * @param parent
	 *            The parent component
	 * @param id
	 *            The component id
	 */
	public DefaultAbstractTree(MarkupContainer parent, String id)
	{
		super(parent, id);
		init();
	}

