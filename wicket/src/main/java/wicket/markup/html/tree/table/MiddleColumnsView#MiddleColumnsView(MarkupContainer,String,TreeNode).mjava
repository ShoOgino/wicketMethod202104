	/**
	 * Constructor.
	 * 
	 * @param parent
	 *            The parent component
	 * @param id
	 *            The component id
	 * @param node
	 *            The tree node
	 */
	public MiddleColumnsView(MarkupContainer parent, String id, TreeNode node)
	{
		super(parent, id);
		this.node = node;
	}

