	/**
	 * Get the wicket:id of the underlying tag
	 * 
	 * @return id
	 */
	public final String getId()
	{
		final ComponentTag tag = getTag();
		if (tag != null)
		{
			return tag.getId();
		}

		return null;
	}

