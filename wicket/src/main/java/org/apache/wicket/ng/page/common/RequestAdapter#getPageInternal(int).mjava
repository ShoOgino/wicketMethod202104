	protected final ManageablePage getPageInternal(int id)
	{
		ManageablePage page = findPage(id);
		if (page == null)
		{
			page = getPage(id);
		}
		if (page != null)
		{
			pages.add(page);	
		}			
		return page;
	}

