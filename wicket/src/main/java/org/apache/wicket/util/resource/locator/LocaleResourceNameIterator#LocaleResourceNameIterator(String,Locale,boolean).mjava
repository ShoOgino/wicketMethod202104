	/**
	 * Construct.
	 * 
	 * @param path
	 * @param locale
	 * @param strict
	 */
	public LocaleResourceNameIterator(final String path, final Locale locale, boolean strict)
	{
		this.path = path;
		this.locale = locale;
		this.strict = strict;
	}

