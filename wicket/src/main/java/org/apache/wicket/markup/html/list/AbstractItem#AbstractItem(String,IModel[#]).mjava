	/**
	 * Constructor
	 * 
	 * @param id
	 *            component id
	 * @param model
	 *            model for this item
	 */
	public AbstractItem(final String id, final IModel<?> model)
	{
		super(id.intern(), model);
	}

