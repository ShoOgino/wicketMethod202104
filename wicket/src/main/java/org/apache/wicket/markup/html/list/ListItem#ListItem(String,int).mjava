	/**
	 * Constructor
	 * 
	 * @param id
	 *            component id
	 * @param index
	 *            relative index of this item in the pageable view
	 */
	public ListItem(final String id, final int index)
	{
		super(id, index);
	}

