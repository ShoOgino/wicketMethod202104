	/**
	 * Sets the index of this item
	 * 
	 * @param index
	 *            new index
	 */
	public final void setIndex(int index)
	{
		if (this.index != index)
		{
			if (isVersioned())
			{
				addStateChange();
			}
			this.index = index;
		}
	}

