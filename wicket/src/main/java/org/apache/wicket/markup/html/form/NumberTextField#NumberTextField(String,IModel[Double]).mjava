	/**
	 * Construct.
	 * 
	 * @param id
	 *            see Component
	 * @param model
	 *            the model
	 */
	public NumberTextField(String id, IModel<Double> model)
	{
		super(id, model, Double.class);

		validator = null;
		minimum = null;
		maximum = null;
	}

