	/**
	 * Constructor
	 * 
	 * @param id
	 *            component id
	 * @param model
	 *            model for this item
	 */
	public AbstractItem(final int id, final IModel<?> model)
	{
		this(Integer.toString(id), model);
	}

