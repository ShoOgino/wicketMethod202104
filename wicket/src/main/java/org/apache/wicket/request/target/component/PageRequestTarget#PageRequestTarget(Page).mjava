	/**
	 * Construct.
	 * 
	 * @param page
	 *            the page instance
	 */
	public PageRequestTarget(Page page)
	{
		if (page == null)
		{
			throw new IllegalArgumentException("Argument page must be not null");
		}

		this.page = page;
	}

