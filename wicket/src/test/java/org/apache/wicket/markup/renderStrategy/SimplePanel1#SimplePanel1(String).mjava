	/**
	 * Construct.
	 * 
	 * @param id
	 * @param prefix
	 */
	public SimplePanel1(final String id)
	{
		super(id);

		add(HeaderContributor.forCss(getClass().getSimpleName() + ".css"));

		addAll("");

		addAll("pre_");
	}

