		/**
		 * Constructor
		 * 
		 * @param id
		 *            Name of form
		 */
		public TestForm(MarkupContainer parent, final String id)
		{
			super(parent, id);

			add(new TextField(this, "input", new Model("test")));
		}

