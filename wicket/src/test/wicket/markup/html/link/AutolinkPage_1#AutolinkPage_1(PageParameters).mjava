	/**
	 * Construct.
	 * @param parameters
	 */
	public AutolinkPage_1(final PageParameters parameters) 
	{
	    add(new Label("myLabel", "Home"));
	    getSession().setStyle("myStyle");
	    getSession().setLocale(Locale.GERMANY);
    }

