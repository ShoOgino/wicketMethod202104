		/**
		 * Construct.
		 * @param id
		 * @param input
		 */
		public MyTextField(final String id, final String input)
		{
			super(id);
			this.input = input;
			
			add(RequiredValidator.getInstance());
		}

