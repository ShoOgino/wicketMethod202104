    private void addUser(List data, int id, String name, String email)
    {
        User user = new User();
        user.id = id;
        user.name = name;
        user.email = email;
        data.add(user);
    }

