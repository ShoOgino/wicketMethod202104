    /**
     * Encode the redirectLocation URL. Does no changes as this test implementation
     * uses cookie based url tracking.
     *
     * @param url The url to encode
     * @return The encoded url
     */
    public String encodeRedirectUrl(final String url) {
        return url;
    }

