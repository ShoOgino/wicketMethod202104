    /**
     * Add an expected child to this tag. Children must be added
     * in the order they are expected to appear.
     *
     * @param e The element to add
     */
    public void addExpectedChild(final DocumentElement e) {
        expectedChildren.add(e);
    }

