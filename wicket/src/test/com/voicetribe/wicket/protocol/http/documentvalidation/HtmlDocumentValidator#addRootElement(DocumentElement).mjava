    /**
     * Add a root element to the validator. This will generally
     * be the HTML element to which all children are added. However,
     * it may also be other elements to represent comments or similar.
     *
     * @param e The element to add
     */
    public void addRootElement(final DocumentElement e) {
        elements.add(e);
    }

