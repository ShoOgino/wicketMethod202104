	/**
	 * Creates a dynamic resource
	 *  
	 * @param idle
	 *          The idle duration timeout
	 * 
	 */
	public DynamicByteArrayResource(Duration idle)
	{
		super(idle);
		setCacheable(false);
	}

