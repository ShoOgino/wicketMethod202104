	/**
	 * 
	 */
	public void submit()
	{
		checkClosed();
		try
		{
			mockWebApp.getServletRequest().setRequestToComponent(workingForm);
			mockWebApp.processRequestCycle();
		}
		finally
		{
			closed = true;
		}
	}

