	/**
	 * @param clz
	 * @param exp
	 * @return
	 */
	private static Field findField(Class clz, String exp)
	{
		Field field = null;
		try
		{
			field = clz.getField(exp);
		}
		catch (Exception ex)
		{
			// TODO log
		}
		return field;
	}

