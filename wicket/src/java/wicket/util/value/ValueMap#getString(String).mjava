    /**
     * Gets a string.
     * @param key The get
     * @return The string
     */
    public final String getString(final String key)
    {
        final Object o = getStringValue(key);

        if (o == null)
        {
            return null;
        }
        else
        {
            return o.toString();
        }
    }

