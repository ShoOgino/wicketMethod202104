	/**
	 * Copy constructor.
	 * 
	 * @param map
	 *            The map to copy
	 */
	public ValueMap(final Map<? extends String, ? extends Object> map)
	{
		super((int) (map.size()/0.75)+1);
		super.putAll(map);
	}

