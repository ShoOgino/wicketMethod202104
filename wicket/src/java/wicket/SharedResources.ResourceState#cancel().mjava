		/**
		 * Cancel all timer task (if any) 
		 */
		void cancel()
		{
			if (idleTask != null)
			{
				idleTask.cancel();
				idleTask = null;
			}
			if (cacheTask != null)
			{
				cacheTask.cancel();
				cacheTask = null;
			}
		}

