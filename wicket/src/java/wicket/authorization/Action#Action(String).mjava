	/**
	 * Construct.
	 * 
	 * @param name
	 *            the action
	 */
	public Action(String name)
	{
		if (name == null)
		{
			throw new IllegalArgumentException("Name argument may not be null");
		}

		this.name = name;
	}

