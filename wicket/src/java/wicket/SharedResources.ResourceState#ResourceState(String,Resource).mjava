		/**
		 * Construct.
		 * 
		 * @param key
		 *            The resource key
		 * @param resource
		 *            The resource
		 */
		ResourceState(String key, Resource resource)
		{
			this.resource = resource;
			makeIdleTask(key);
			makeCacheTask(key);
		}

