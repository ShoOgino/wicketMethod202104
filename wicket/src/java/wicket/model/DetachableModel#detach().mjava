    /**
     * Detaches from the current session.
     * 
     * @see wicket.model.IDetachableModel#detach()
     */
    public final void detach()
    {
        if (attached)
        {
            doDetach();
            attached = false;
        }
    }

