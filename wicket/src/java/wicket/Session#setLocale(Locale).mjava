	/**
	 * Set the locale.
	 * 
	 * @param locale
	 *            New locale
	 */
	public final void setLocale(final Locale locale)
	{
		this.state.locale = locale;
		this.converter = null;
		dirty();
	}

