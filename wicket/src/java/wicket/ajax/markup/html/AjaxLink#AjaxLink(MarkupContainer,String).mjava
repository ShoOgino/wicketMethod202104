	/**
	 * Construct.
	 * 
	 * @param parent
	 *            The parent of this component
	 * 
	 * @param id
	 */
	public AjaxLink(MarkupContainer parent, final String id)
	{
		this(parent, id, null);
	}

