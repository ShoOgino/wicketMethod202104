	/**
	 * Construct.
	 * 
	 * @param parent
	 *            The parent of this component
	 * 
	 * @param id
	 */
	public AjaxFallbackLink(MarkupContainer parent, final String id)
	{
		this(parent, id, null);
	}

