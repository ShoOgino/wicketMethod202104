		/**
		 * @see java.util.Iterator#remove()
		 */
		public void remove()
		{
			iterator.remove();
			// Notify Page
			final Page page = findPage();
			if (page != null)
			{
				page.componentRemoved(component);
			}
		}

