	/**
	 * @param page
	 *            The page to add
	 * @return Any Page that got bumped out of the map
	 */
	final Page add(final Page page)
	{
		// Give page a new id
		page.setId(this.pageId++);
		session.dirty();

		// Add to map
		return put(page);
	}

