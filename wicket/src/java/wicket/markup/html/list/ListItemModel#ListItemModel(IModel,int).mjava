	/**
	 * Construct
	 * 
	 * @param listViewModel
	 * @param index
	 */
	public ListItemModel(final IModel listViewModel, final int index)
	{
		this.listViewModel = listViewModel;
		this.index = index;
		attach();
	}

