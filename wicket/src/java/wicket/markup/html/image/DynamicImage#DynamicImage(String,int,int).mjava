	/**
	 * Constructor.
	 * 
	 * @param name
	 *            Component name
	 * @param width
	 *            Width of image
	 * @param height
	 *            Height of image
	 */
	public DynamicImage(String name, int width, int height)
	{
		super(name);
		this.width = width;
		this.height = height;
	}

