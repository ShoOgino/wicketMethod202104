    /**
     * Attach to the current request.
     * @see wicket.markup.html.form.IIdList#attach()
     */
    public final void attach()
    {
        if (!attached)
        {
            doAttach();
            attached = true;
        }
    }

