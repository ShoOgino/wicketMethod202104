		/**
		 * Constructor
		 * 
		 * @param object
		 *            The object
		 * @param index
		 *            The index of the object in the choice list
		 */
		public Choice(final Object object, final int index)
		{
			this.object = object;
			this.index = index;
		}

