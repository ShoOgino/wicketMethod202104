	/**
	 * @see wicket.markup.html.form.FormComponent#getForm()
	 */
	@Override
	public Form getForm()
	{
		if (this.form != null)
		{
			return this.form;
		}
		else
		{
			return findParent(Form.class);
		}
	}

