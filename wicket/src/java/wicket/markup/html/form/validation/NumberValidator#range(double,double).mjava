    /**
     * Gets a Double range validator to check if a number is 
     * between the mininum and maximum value.
     * 
     * If that is not the case then an error message will be generated
     * with the key "NumberValidator.range" and the messages keys that 
     * can be used are:
     * <ul>
     * <li>${minimum}: the minimal value</li>
     * <li>${maximum}: the maximum value</li>
     * </ul>
     * 
     * @param minimum The minimum value.
     * @param maximum The maximum value.
     * 
     * @return The NumberValidator
     */
    public static NumberValidator range(double minimum, double maximum)
    {
       return new DoubleRangeValidator(minimum,maximum);
    }

