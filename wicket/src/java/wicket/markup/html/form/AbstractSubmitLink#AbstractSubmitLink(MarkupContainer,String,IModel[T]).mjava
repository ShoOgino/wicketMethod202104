	/**
	 * Construct.
	 * 
	 * @param parent
	 * @param id
	 * @param model
	 */
	public AbstractSubmitLink(MarkupContainer parent, String id, IModel<T> model)
	{
		super(parent, id, model);
	}

