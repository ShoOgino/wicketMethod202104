	/**
	 * Construct.
	 * 
	 * @param id
	 *            component id
	 * @param fc
	 *            form component
	 */
	public SimpleFormComponentLabel(String id, FormComponent fc)
	{
		super(id, fc);
		setModel(fc.getLabel());
	}

