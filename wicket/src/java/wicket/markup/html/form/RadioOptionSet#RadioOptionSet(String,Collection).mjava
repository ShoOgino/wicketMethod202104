	/**
	 * Constructor.
	 * 
	 * @param name
	 *            The component name
	 * @param values
	 *            The option values
	 */
	public RadioOptionSet(final String name, final Collection values)
	{
		this(name, values, LINE_BREAK);
	}

