	/**
	 * Construct.
	 * 
	 * @param page
	 *            The webpage where the body container is in
	 * @param id
	 *            The container id
	 */
	public BodyContainer(final WebPage page, final String id)
	{
		this.page = page;
		this.id = id;
	}

