	/**
	 * Constructor.
	 * @param parent 
	 * @param id 
	 * @param node 
	 */
	public SideColumnsView(MarkupContainer parent, String id, TreeNode node) {
		super(parent, id);
		setRenderBodyOnly(true);
		this.node = node;
	}

