		/**
		 * Construct.
		 * 
		 * @param name
		 *            name of the component
		 * @param model
		 *            the model
		 */
		public TreePathsListView(String name, IModel model)
		{
			super(name, model);
		}

