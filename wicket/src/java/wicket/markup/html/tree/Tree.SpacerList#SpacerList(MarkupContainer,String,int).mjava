		/**
		 * Construct.
		 * 
		 * @param id
		 *            component id
		 * @param size
		 *            size of loop
		 */
		public SpacerList(MarkupContainer parent, String id, int size)
		{
			super(parent, id, size);
		}

