		/**
		 * The highest visible nodes have a depth of 0.
		 * @return the level of visibility
		 */
		public int getVisibleLevel()
		{
			if (isRootVisible())
			{
				return getLevel();
			}
			else
			{
				return getLevel() - 1;
			}
		}

