	/**
	 * Sign in user if possible.
	 * 
	 * @param cycle
	 *           The request cycle
	 * @param username
	 *           The username
	 * @param password
	 *           The password
	 * @return Error message to display, or null if the user was signed in
	 */
	public abstract String signIn(final RequestCycle cycle, final String username, final String password);

