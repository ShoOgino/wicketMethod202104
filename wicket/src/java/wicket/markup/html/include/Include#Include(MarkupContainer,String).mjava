	/**
	 * Construct.
	 * 
	 * @param parent
	 *            The parent of this component
	 * 
	 * @param id
	 *            component id
	 */
	public Include(MarkupContainer parent, final String id)
	{
		super(parent, id);
	}

