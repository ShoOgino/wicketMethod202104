	/**
	 * Construct.
	 * 
	 * @param id
	 *            component id
	 * @param model
	 *            the model
	 */
	public Include(MarkupContainer<?> parent,String id, IModel model)
	{
		super(parent,id, model);
	}

