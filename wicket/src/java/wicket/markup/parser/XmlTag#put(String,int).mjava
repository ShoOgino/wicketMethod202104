	/**
	 * Puts an int attribute.
	 * 
	 * @param key
	 *           The key
	 * @param value
	 *           The value
	 */
	public void put(final String key, final int value)
	{
		put(key, Integer.toString(value));
	}

