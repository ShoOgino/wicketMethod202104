	/**
	 * @see javax.servlet.ServletResponse#getLocale()
	 */
	public Locale getLocale()
	{
		isOpen();
		if (this.locale == null)
		{
			return realResponse.getLocale();
		}
		return this.locale;
	}

