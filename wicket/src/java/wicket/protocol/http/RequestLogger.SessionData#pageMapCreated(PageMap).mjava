		/**
		 * @param map
		 */
		public void pageMapCreated(PageMap map)
		{
			getCurrentRequest()
					.addEntry(
							"PageMap created, name: "
									+ (map.getName() == null ? "DEFAULT" : map.getName()));
		}

