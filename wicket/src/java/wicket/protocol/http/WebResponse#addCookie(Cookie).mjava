    /**
     * Adds a cookie.
     * 
     * @param cookie
     *            The cookie to add
     */
    public final void addCookie(final Cookie cookie)
    {
        httpServletResponse.addCookie(cookie);
    }

