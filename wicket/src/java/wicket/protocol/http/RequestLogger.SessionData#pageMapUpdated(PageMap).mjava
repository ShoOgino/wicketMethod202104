		/**
		 * @param map
		 */
		public void pageMapUpdated(PageMap map)
		{
			getCurrentRequest()
					.addEntry(
							"PageMap updated, name: "
									+ (map.getName() == null ? "DEFAULT" : map.getName()));
		}

