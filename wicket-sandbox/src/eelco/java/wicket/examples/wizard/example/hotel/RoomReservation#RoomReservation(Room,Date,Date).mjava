	/**
	 * Construct.
	 * 
	 * @param room
	 *            room
	 * @param from
	 *            from date
	 * @param until
	 *            until date
	 */
	public RoomReservation(Room room, Date from, Date until)
	{
		this.room = room;
		this.from = from;
		this.until = until;
	}

