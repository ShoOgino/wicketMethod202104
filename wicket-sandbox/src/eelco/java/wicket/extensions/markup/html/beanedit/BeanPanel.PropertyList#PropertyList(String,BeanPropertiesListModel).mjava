		/**
		 * Construct.
		 * 
		 * @param id
		 *            component name
		 * @param model
		 *            the model
		 */
		public PropertyList(String id, BeanPropertiesListModel model)
		{
			super(id, model);
			setReuseItems(true);
		}

