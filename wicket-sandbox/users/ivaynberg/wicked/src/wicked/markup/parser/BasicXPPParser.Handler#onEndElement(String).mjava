		public void onEndElement(String name) {
			if (componentTags.peek() == true) {

				recordStaticMarkup();

				endComponent();

			} else {
				if (!emptyTags.peek()) {
					textBuffer.append("</").append(name).append(">");
				}
			}

			componentTags.pop();
			
			emptyTags.pop();

		}

