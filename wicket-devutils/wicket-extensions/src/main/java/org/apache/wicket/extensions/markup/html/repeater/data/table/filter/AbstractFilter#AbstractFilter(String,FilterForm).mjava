	/**
	 * @param id
	 *            component id
	 * @param form
	 *            filter form of the filter toolbar
	 */
	public AbstractFilter(String id, FilterForm form)
	{
		super(id);
		this.form = form;
	}

