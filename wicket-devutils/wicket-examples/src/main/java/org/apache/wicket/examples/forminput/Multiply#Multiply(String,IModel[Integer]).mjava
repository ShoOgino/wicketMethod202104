	/**
	 * Construct.
	 * 
	 * @param id
	 *            The component id
	 * @param model
	 *            The model
	 */
	public Multiply(String id, IModel<Integer> model)
	{
		super(id, model);
		init();
	}

