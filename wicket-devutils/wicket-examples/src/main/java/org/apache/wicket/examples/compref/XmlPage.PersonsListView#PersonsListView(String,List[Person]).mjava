		/**
		 * Construct.
		 * 
		 * @param id
		 *            component id
		 * @param list
		 *            the model
		 */
		public PersonsListView(String id, List<Person> list)
		{
			super(id, list);
		}

