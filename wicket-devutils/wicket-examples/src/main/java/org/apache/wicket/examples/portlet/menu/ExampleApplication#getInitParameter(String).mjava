	/**
	 * Gets initParameter.
	 * 
	 * @param name
	 *            initParameter name
	 * @return initParameter
	 */
	public String getInitParameter(String name)
	{
		return initParameters.get(name);
	}

