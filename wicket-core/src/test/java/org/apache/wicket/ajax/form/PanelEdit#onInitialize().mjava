    @Override
    protected void onInitialize() {
        super.onInitialize();

        Form<Void> form = new Form<>("form", null);
        add(form);

        form.add(createSubmitButton());
    }

