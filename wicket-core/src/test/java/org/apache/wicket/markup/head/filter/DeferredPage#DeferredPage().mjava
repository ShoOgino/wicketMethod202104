	/**
	 * Construct.
	 * 
	 * @param parameters
	 */
	public DeferredPage()
	{
		add(new AjaxEventBehavior("click") {
			@Override
			protected void onEvent(AjaxRequestTarget target)
			{
			}
		});
	}

