	/**
	 * 
	 * @param child
	 * @return child
	 */
	final MarkupContainer add(final Component child)
	{
		if (borderMustBeInitialized())
		{
			initializeBorder();
		}
		addToBorder(child);
		return this;
	}

