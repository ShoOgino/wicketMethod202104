	/**
	 * Constructor.
	 * 
	 * @param name
	 *            the cookie name
	 * @param value
	 *            the cookie value
	 */
	public CookiePage(String name, String value)
	{
		cookieName = name;
		cookieValue = value;

		doAssert();
	}

