	/**
	 * Construct.
	 * 
	 * @param scope
	 *            mandatory parameter
	 * @param name
	 *            mandatory parameter
	 */
	public CssResourceReference(Class<?> scope, String name)
	{
		super(scope, name);
	}

