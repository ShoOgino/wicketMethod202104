	/**
	 * Stores the page-
	 * 
	 * @param context
	 *            the context of the page
	 * @param id
	 *            the id of the page.
	 */
	void addPage(IPageContext context, IManageablePage page);

