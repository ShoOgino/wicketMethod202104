	/**
	 * Store the page data
	 * 
	 * @param sessionId
	 *            Session ID
	 * @param id
	 *            Page ID
	 * @param data
	 *            Page data
	 */
	void storeData(String sessionId, int id, byte[] data);

