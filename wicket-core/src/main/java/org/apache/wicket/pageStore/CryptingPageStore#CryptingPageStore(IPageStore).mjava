	/**
	 * @param delegate
	 *            store to delegate to
	 * @param applicationName
	 *            name of application
	 */
	public CryptingPageStore(IPageStore delegate)
	{
		super(delegate);
	}

