		@Override
		public int compareTo(SessionData sd)
		{
			int result = 0;
			if (sd.lastActive > lastActive)
			{
				result = 1;
			}
			else if (sd.lastActive < lastActive)
			{
				result = -1;
			}
			return result;
		}

