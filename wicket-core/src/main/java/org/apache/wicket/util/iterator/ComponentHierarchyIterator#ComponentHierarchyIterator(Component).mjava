	/**
	 * Construct.
	 * 
	 * @param component
	 *            Iterate over the containers children
	 */
	public ComponentHierarchyIterator(final Component component)
	{
		super(component);
	}

