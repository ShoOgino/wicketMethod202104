	/**
	 * Construct.
	 * 
	 * @param id
	 *            the component id
	 * @param model
	 *            the link's model
	 * @param labelModel
	 */
	public AbstractLink(String id, IModel<?> model, final IModel<?> labelModel)
	{
		super(id, model);
		label = wrap(labelModel);
	}

