	/**
	 * Constructor.
	 *
	 * @param id
	 *          The component id
	 * @param model
	 *          The component model
	 */
	public MediaComponent(String id, IModel<?> model)
	{
		this(id, model, null, null, null);
	}

