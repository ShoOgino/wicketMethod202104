	/**
	 * Construct.
	 * 
	 * @param id
	 *            component id
	 * @param url
	 *            the url input value
	 */
	public UrlTextField(String id, final String url)
	{
		this(id, new Model<String>(url));
	}

