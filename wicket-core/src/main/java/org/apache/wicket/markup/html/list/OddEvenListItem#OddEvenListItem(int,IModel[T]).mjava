	/**
	 * Constructor
	 * 
	 * @param index
	 *            list item's index
	 * @param model
	 *            list item's model
	 */
	public OddEvenListItem(int index, IModel<T> model)
	{
		super(index, model);
	}

