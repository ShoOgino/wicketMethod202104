	/**
	 * Creates a source
	 * 
	 * @param id
	 *            the component id
	 * @param model
	 *            the internally used model
	 */
	public Source(String id, IModel<?> model)
	{
		this(id, model, null, null, null);
	}

