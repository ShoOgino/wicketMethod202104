	/**
	 * Construct.
	 * 
	 * @param id
	 *            the component id
	 * @param model
	 *            the link's model
	 */
	public AbstractLink(String id, IModel<?> model)
	{
		this(id, model, null);
	}

