	/**
	 * Construct.
	 * 
	 * @param id
	 *            see Component
	 * @param model
	 *            the input value
	 */
	public NumberTextField(String id, IModel<N> model)
	{
		super(id, model);

		validator = null;
		minimum = null;
		maximum = null;
	}

