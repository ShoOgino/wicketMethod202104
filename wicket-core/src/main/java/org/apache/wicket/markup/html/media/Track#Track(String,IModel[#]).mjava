	/**
	 * Creates a track
	 * 
	 * @param id
	 *            the component id
	 * @param model
	 *            the internally used model
	 */
	public Track(String id, IModel<?> model)
	{
		this(id, model, null, null, null);
	}

