	/**
	 * Constructor
	 * 
	 * @param id
	 *            component id
	 * @param model
	 *            model for this item
	 */
	public AbstractItem(final long id, final IModel<?> model)
	{
		this(Long.toString(id), model);
	}

