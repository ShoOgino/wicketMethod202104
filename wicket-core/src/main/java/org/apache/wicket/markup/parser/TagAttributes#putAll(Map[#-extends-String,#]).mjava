	@Override
	public final void putAll(Map<? extends String, ?> map)
	{
		for (Object o : map.keySet())
		{
			String key = (String)o;
			checkIdAttribute(key);
		}

		super.putAll(map);
	}

