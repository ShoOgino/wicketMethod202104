	/**
	 * Constructor
	 * 
	 * @param id
	 *            component id
	 * @param index
	 *            relative index of this item in the pageable view
	 */
	public LoopItem(final String id, final int index)
	{
		super(id);
		this.index = index;
	}

