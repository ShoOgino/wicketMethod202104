	/**
	 * Constructor.
	 * 
	 * @param id
	 *            component id
	 */
	public BrowserInfoForm(String id)
	{
		super(id);

		this.form = createForm("postback");
		form.setOutputMarkupId(true);
		add(form);
	}

