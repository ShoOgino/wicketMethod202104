	/**
	 * Called for every component after its {@code Component#onConfigure()} and
	 * {@link org.apache.wicket.behavior.Behavior#onConfigure(org.apache.wicket.Component)}
	 * methods have been executed).
	 *
	 * @param component
	 *            the component that is being configured.
	 */
	void onConfigure(Component component);

