	/**
	 * Creates a new {@code MinifiedAwareCssResourceReference}
	 * 
	 * @param scope
	 *            mandatory parameter
	 * @param name
	 *            mandatory parameter
	 */
	public MinifiedAwareCssResourceReference(Class<?> scope, String name)
	{
		super(scope, name);
	}

