	/**
	 * Copy constructor.
	 * 
	 * @param map
	 *            the <code>ValueMap</code> to copy
	 */
	public ValueMap(final Map<? extends String, ? extends Object> map)
	{
		super();

		super.putAll(map);
	}

