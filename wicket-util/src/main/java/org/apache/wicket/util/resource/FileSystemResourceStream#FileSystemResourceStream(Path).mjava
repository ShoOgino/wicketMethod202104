	/**
	 * Constructor.
	 * 
	 * @param path
	 *            {@link Path} containing resource
	 */
	public FileSystemResourceStream(final Path path)
	{
		Args.notNull(path, "path");
		this.path = path;
	}

