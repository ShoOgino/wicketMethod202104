	/**
	 * Gets the first value in the value list
	 * 
	 * @param key
	 * @return
	 */
	public V getFirstValue(K key)
	{
		List<V> list = get(key);
		if (list != null && !list.isEmpty())
		{
			return list.get(0);
		}
		return null;
	}

