	/**
	 * Constructor
	 * 
	 * @param folders
	 *            An array of folders to add to the path
	 */
	public Path(final Folder[] folders)
	{
		if (folders != null)
		{
			for (Folder folder : folders)
			{
				add(folder);
			}
		}
	}

