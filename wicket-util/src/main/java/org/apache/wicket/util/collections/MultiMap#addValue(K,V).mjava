	/**
	 * Adds value to the specified key
	 * 
	 * @param key
	 * @param value
	 */
	public void addValue(K key, V value)
	{
		List<V> list = get(key);
		if (list == null)
		{
			list = new ArrayList<V>(1);
		}
		list.add(value);
		put(key, list);
	}

