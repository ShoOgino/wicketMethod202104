	/**
	 * Gets initParameter.
	 * 
	 * @param name
	 *            initParameter name
	 * @return initParameter
	 */
	public String getInitParameter(String name)
	{
		return (String)initParameters.get(name);
	}

