	/**
	 * Constructor
	 * 
	 * @param application
	 *            The application
	 * @param request
	 */
	protected SignInSession(final WebApplication application, Request request)
	{
		super(application, request);
	}

