       /**
        *
        * @param id
        * @param form
        */
       public IndicatingAjaxButton(String id, Form form)
       {
               super(id, form);
               add(indicatorAppender);
       }

