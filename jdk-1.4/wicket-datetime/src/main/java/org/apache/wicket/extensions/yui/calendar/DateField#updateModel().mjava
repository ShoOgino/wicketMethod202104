	/**
	 * @see org.apache.wicket.markup.html.form.FormComponent#updateModel()
	 */
	public void updateModel()
	{

		if (date != null)
		{
			Date d = date.toDate();
			setModelObject(d);
		}
		else
		{
			setModelObject(null);
		}
	}

