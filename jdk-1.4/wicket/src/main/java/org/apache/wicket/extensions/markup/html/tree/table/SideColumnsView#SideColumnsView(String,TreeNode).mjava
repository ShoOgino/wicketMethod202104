	/**
	 * Constructor.
	 * 
	 * @param id
	 *            The component id
	 * @param node
	 *            The tree node
	 */
	public SideColumnsView(String id, TreeNode node)
	{
		super(id);
		setRenderBodyOnly(true);
		this.node = node;
	}

