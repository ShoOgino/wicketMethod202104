	/**
	 * Detaches from the given session
	 */
	protected final void onDetach()
	{
		// Detach any model
		if (model != null)
		{
			model.detach();
		}

		// Null out references
		localizer = null;
		locale = null;
	}

