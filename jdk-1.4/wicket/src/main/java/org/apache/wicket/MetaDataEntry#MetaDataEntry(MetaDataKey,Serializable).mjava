	/**
	 * Construct.
	 * 
	 * @param key
	 *            meta data key
	 * @param object
	 */
	public MetaDataEntry(MetaDataKey key, Serializable object)
	{
		this.key = key;
		this.object = object;
	}

