	/**
	 * Construct.
	 * 
	 * @param path
	 * @param locale
	 * @param style
	 */
	public LocaleResourceNameIterator(final String path, final Locale locale)
	{
		this.path = path;
		this.locale = locale;
	}

