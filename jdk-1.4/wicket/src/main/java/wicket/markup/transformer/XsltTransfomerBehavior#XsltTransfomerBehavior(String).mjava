	public XsltTransfomerBehavior(final String xslFilePath)
	{
		throw new WicketRuntimeException(XsltTransfomerBehavior.class.getName() + " has been renamed to " + XsltTransformerBehavior.class.getName());
	}

