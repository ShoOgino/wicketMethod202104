	/**
	 * Construct.
	 * 
	 * @param roles
	 *            roles as a comma seperated list, like ADMIN,USER
	 */
	public Roles(String roles)
	{
		this.wrapped = roles;
		String[] r = getWrapped();
		for (String role : r)
		{
			this.roles.add(role);
		}
	}

