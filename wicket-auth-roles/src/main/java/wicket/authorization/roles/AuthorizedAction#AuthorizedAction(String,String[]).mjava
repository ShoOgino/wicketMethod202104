	/**
	 * Construct.
	 * 
	 * @param action
	 *            the auth action
	 * @param roles
	 *            the roles
	 */
	public AuthorizedAction(String action, String[] roles)
	{
		this(new Action(action), roles);
	}

