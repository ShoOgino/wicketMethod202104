	/**
	 * Construct.
	 * 
	 * @param action
	 *            the auth action
	 * @param roles
	 *            the roles
	 */
	public ActionRoles(Action action, String[] roles)
	{
		this.action = action;
		this.roles = roles;
	}

