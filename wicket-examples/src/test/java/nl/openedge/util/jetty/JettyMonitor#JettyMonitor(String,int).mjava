	/**
	 * Hidden constructor.
	 * 
	 * @param commKey
	 *            auth key
	 * @param monitorPort
	 *            monitor port
	 */
	private JettyMonitor(String commKey, int monitorPort)
	{
		this.commKey = commKey;
		this.monitorPort = monitorPort;
	}

