		/**
		 * @param inserts
		 * @param deletes
		 * @return change letter
		 */
		public static char change_letter(int inserts, int deletes)
		{
			if (inserts == 0)
				return 'd';
			else if (deletes == 0)
				return 'a';
			else
				return 'c';
		}

