	/**
	 * Construct.
	 * 
	 * @param parent
	 * @param id
	 *            The component id
	 * @param model
	 *            The model
	 */
	public Multiply(final MarkupContainer parent, String id, IModel<Integer> model)
	{
		super(parent, id, model);
		init();
	}

