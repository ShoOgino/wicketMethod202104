		/**
		 * Construct.
		 * 
		 * @param parent
		 * @param id
		 */
		public LinesListView(final MarkupContainer parent, final String id)
		{
			super(parent, id);
			// always do this in forms!
			setReuseItems(true);
		}

