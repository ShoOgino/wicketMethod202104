	/**
	 * Constructor
	 * 
	 * @param application
	 *            The application
	 * @param request
	 *            The current request
	 */
	protected SignIn2Session(final WebApplication application, Request request)
	{
		super(application, request);
	}

