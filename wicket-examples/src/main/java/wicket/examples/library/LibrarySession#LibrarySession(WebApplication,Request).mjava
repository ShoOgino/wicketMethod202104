	/**
	 * Constructor
	 * 
	 * @param application
	 *            The application
	 * @param request
	 *            The current request
	 */
	protected LibrarySession(final WebApplication application, Request request)
	{
		super(application, request);
	}

