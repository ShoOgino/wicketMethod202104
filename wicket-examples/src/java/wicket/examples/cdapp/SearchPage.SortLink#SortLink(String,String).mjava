		/**
		 * Construct.
		 * 
		 * @param componentName
		 *            name of component
		 * @param field
		 *            order by field
		 */
		public SortLink(String componentName, String field)
		{
			super(componentName);
			this.field = field;
		}

