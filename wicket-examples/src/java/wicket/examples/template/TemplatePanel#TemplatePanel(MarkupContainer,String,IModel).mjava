	/**
	 * Construct.
	 * 
	 * @param id
	 *            component id
	 * @param model
	 *            the model
	 */
	public TemplatePanel(MarkupContainer parent,final String id, IModel model)
	{
		super(parent,id, model);
	}

