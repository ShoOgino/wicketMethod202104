	/**
	 * Construct.
	 * 
	 * @param id
	 *            component id
	 * @param model
	 *            the model
	 */
	public Panel1(MarkupContainer parent,final String id, IModel model)
	{
		super(parent,id, model);
	}

