    /**
     * Constructor.
     */
    public BookmarkablePage()
    {
    	this(null);
    	// note: its here for style actually. When you have both a default constructor,
    	// and a constructor with a PageParameters argument, the latter is allways called
    	// with a non-null argument
	}

