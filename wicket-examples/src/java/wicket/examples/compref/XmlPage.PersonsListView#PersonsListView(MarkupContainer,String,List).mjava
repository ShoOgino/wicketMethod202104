		/**
		 * Construct.
		 * 
		 * @param id
		 *            component id
		 * @param list
		 *            the model
		 */
		public PersonsListView(MarkupContainer parent,final String id, List list)
		{
			super(parent,id, list);
		}

