		/**
		 * Construct.
		 * 
		 * @param name
		 *            component name
		 * @param object
		 *            model
		 */
		public LocalImage(String name, Serializable object)
		{
			super(name, new Model(object));
		}

