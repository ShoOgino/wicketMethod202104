    /**
     * Gets the tree model.
     * @return the tree model
     */
    public TreeModel getFileModel()
    {
        synchronized(lock)
        {
            if(model == null)
            {
                model = buildTree();
            }
        }
        return model;
    }

