		/**
		 * Construct.
		 * @param componentName
		 * @param label
		 */
		public LabelPanel(String componentName, String label)
		{
			super(componentName);
			add(new Label("my_label", label));
		}

