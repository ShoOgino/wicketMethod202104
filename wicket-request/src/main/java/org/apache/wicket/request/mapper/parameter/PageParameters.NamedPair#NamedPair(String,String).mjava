		/**
		 * Constructor
		 * 
		 * @param key
		 * @param value
		 */
		public NamedPair(final String key, final String value)
		{
			Args.notEmpty(key, "key");
			this.key = key;
			this.value = value;
		}

