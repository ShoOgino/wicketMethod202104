	/**
	 * Construct.
	 * 
	 * @param id
	 *      the component id
	 * @param model
	 *      the component model
	 */
	public DefaultCssAutoCompleteTextField(final String id, final IModel<T> model)
	{
		super(id, model);
	}

