	/**
	 * Wrap the given model.
	 * 
	 * @param model
	 *            model to wrap
	 * @param branches
	 */
	public NodeModel(IModel<T> model, boolean[] branches)
	{
		this.model = model;
		this.branches = branches;
	}

