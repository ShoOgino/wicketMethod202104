	/**
	 * {@inheritDoc}
	 */
	@Override
	protected void onDetach()
	{
		if (choices != null)
		{
			choices.detach();
		}
		
		if (renderer != null)
		{
			renderer.detach();
		}
		
		super.onDetach();
	}

