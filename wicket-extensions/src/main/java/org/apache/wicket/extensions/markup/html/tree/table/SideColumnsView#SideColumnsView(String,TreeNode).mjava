	/**
	 * Constructor.
	 * 
	 * @param id
	 *            The component id
	 * @param node
	 *            The tree node
	 */
	public SideColumnsView(final String id, final TreeNode node)
	{
		super(id, node);
		setRenderBodyOnly(true);
	}

