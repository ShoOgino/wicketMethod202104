	/**
	 * Constructor
	 * 
	 * @param table
	 *            data table this toolbar will be attached to
	 */
	public AbstractToolbar(String id, DataTable<?> table)
	{
		super(id);
		this.table = table;
	}

