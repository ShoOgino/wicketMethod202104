	/**
	 * Set the model object.
	 * 
	 * @param state
	 *            the model object
	 */
	@Override
	public AbstractTree<T> setModelObject(Set<T> state)
	{
		setDefaultModelObject(state);
		return this;
	}

