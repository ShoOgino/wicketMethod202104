	/**
	 * @param property
	 *            sort property
	 * @param order
	 *            sort order
	 */
	public SortParam(String property, SortOrder order)
	{
		this.property = property;
		this.order = order;
	}

