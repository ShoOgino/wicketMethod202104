	/**
	 * Construct with a pattern.
	 * 
	 * @param id
	 *            the component id
	 * @param pattern
	 *            the pattern to use
	 */
	public LocalDateTextField(String id, String pattern)
	{
		this(id, null, pattern);
	}

