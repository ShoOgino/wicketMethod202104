	/**
	 * Construct.
	 * 
	 * @param id
	 *            the component id
	 * @param node
	 *            The tree node
	 */
	public AbstractColumnsView(String id, final TreeNode node)
	{
		super(id);

		this.node = node;
	}

