	/**
	 * Creates a new modal window component.
	 *
	 * @param id
	 *            Id of component
	 * @param model
	 *            Model
	 */
	public ModalWindow(String id, IModel<T> model)
	{
		super(id, model);
		init();
	}

