	/**
	 * Constructor
	 * 
	 * @param title
	 *            IModel used to represent the title of the tab. Must contain a
	 *            string
	 */
	public AbstractTab(IModel<String> title)
	{
		this.title = title;
	}

