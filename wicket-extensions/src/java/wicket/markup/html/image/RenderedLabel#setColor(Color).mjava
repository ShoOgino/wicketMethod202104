	/**
	 * @param color
	 *            Color to print text
	 * @return this, for chaining
	 */
	public RenderedLabel setColor(Color color)
	{
		this.color = color;
		resource.invalidate();
		return this;
	}

