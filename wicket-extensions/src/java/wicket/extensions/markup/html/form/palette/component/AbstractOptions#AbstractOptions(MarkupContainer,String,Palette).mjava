	/**
	 * @param id
	 *            component id
	 * @param palette
	 *            parent palette
	 */
	public AbstractOptions(MarkupContainer parent,final String id, Palette palette)
	{
		super(parent,id);
		this.palette = palette;
	}

