	/**
	 * @see wicket.extensions.markup.html.basic.tabs.ITab#getPanel(java.lang.String)
	 */
	public Panel getPanel(final String panelId)
	{
		if (panel == null)
		{
			panel = delegate.getPanel(panelId);
		}
		return panel;
	}

