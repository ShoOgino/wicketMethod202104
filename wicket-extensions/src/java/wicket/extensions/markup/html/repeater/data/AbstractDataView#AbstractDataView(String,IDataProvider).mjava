	/**
	 * @param id
	 *            component id
	 * @param dataProvider
	 *            data provider
	 */
	public AbstractDataView(String id, IDataProvider dataProvider)
	{
		super(id, new Model(dataProvider));
	}

