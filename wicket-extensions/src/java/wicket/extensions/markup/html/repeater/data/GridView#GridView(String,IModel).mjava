	/**
	 * @param id
	 *            component id
	 * @param model
	 *            component model - model object must be instance of
	 *            IDataProvider
	 */
	public GridView(String id, IModel model)
	{
		super(id, model);
	}

