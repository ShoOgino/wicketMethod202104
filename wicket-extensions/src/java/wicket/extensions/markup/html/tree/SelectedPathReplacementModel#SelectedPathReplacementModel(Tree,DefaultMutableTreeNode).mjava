	/**
	 * Construct.
	 * 
	 * @param tree
	 *            The tree
	 * 
	 * @param node
	 *            The tree node
	 */
	public SelectedPathReplacementModel(Tree tree, DefaultMutableTreeNode node)
	{
		this.node = node;
		this.tree = tree;
	}

