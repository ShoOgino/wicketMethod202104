	/**
	 * Constructor.
	 * 
	 * @param id
	 *            The component id
	 * @param node
	 *            The tree node
	 */
	public MiddleColumnsView(String id, TreeNode node)
	{
		super(id);
		this.node = node;
	}

