		/**
		 * Construct.
		 * 
		 * @param parent
		 * @param id
		 * @param wizard
		 */
		public Content(MarkupContainer parent, String id, IWizard wizard)
		{
			super(parent, id);
			WizardStep.this.populate(this);
		}

