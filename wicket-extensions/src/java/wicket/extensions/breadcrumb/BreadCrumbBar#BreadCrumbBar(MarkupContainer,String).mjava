	/**
	 * Construct. Adds the default style.
	 * 
	 * @param parent
	 *            Parent component
	 * 
	 * @param id
	 *            Component id
	 */
	public BreadCrumbBar(MarkupContainer parent, final String id)
	{
		this(parent, id, true);
	}

