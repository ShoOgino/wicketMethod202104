	/**
	 * Creates a new DateTextField defaulting to using a short date pattern
	 * 
	 * @param parent
	 *            The parent
	 * @param id
	 *            The id of the text field
	 * 
	 * @see wicket.markup.html.form.TextField
	 */
	public static DateTextField forShortStyle(MarkupContainer parent, String id) {
		return forShortStyle(parent, id, null);
	}

