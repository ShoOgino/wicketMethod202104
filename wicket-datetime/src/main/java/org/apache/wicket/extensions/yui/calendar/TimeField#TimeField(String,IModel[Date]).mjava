	/**
	 * Construct.
	 * 
	 * @param id
	 * @param model
	 */
	public TimeField(String id, IModel<Date> model)
	{
		super(id, model);

		getDateTextField().setVisibilityAllowed(false);
	}

